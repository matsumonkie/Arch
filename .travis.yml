language: ruby
rvm:
  - 2.1.2

before_script:
  - cp config/database.yml.travis config/database.yml

script:
  - RAILS_ENV=test bundle exec rake db:create && bundle exec rake db:schema:load
  - bundle exec rspec